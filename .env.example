# Telegram bot token used to validate init data
BOT_TOKEN=

# Supabase/Postgres connection string (must include sslmode=require)
# DATABASE_URL=postgresql://postgres:password@db.supabase.co:5432/postgres?sslmode=require

# Optional: provide credentials via individual variables (used if DATABASE_URL is not set)
# PGHOST=aws-xxx.supabase.co
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=your-strong-password
# PGDATABASE=postgres
# PGSSLMODE=require

# Allow bypassing Telegram auth (for local development only)
# ALLOW_INSECURE_INIT_DATA=false

# Disable serving the front-end bundle from Express
# SERVE_FRONTEND=true

# Token for admin API access (required for project office dashboard)
# ADMIN_TOKEN=MetaLampPMO
